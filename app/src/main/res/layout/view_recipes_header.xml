<?xml version="1.0" encoding="utf-8"?>
<!-- Recipes page header: dual-mode header that toggles between title/subtitle
     and an active search bar with smooth transitions. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/recipesHeaderRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/space_2"
    android:paddingBottom="@dimen/space_2">

    <!-- Mode 1: Static Title & Subtitle -->
    <LinearLayout
        android:id="@+id/recipesTitleContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:visibility="visible">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/recipesTitle"
                style="@style/TextAppearance.Lighture.PageTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/recipes_title"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/font_size_xl" />

            <TextView
                android:id="@+id/recipesSubtitle"
                style="@style/TextAppearance.Lighture.BodySmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_0_5"
                android:maxLines="2"
                android:text="@string/recipes_subtitle"
                android:textColor="@color/text_secondary" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/recipesSearchButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="@dimen/space_3"
            android:background="@drawable/bg_icon_circle"
            android:contentDescription="@string/recipes_search_description"
            android:padding="@dimen/space_3"
            app:srcCompat="@drawable/ic_search"
            app:tint="@color/icon_primary" />
    </LinearLayout>

    <!-- Mode 2: Active Search Bar -->
    <LinearLayout
        android:id="@+id/recipesSearchBarContainer"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/bg_filter_sort_container"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="@dimen/space_4"
        android:paddingEnd="@dimen/space_1"
        android:visibility="gone">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:contentDescription="@null"
            android:src="@drawable/ic_search"
            app:tint="@color/text_tertiary" />

        <EditText
            android:id="@+id/recipesSearchInput"
            style="@style/TextAppearance.Lighture.Body"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="@dimen/space_2"
            android:layout_weight="1"
            android:background="@null"
            android:hint="@string/recipes_search_hint"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:importantForAutofill="no"
            android:maxLines="1"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_tertiary"
            android:textSize="@dimen/font_size_sm" />

        <ImageButton
            android:id="@+id/recipesSearchClose"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/recipes_search_close"
            android:padding="@dimen/space_2_5"
            app:srcCompat="@drawable/ic_chevron_right"
            app:tint="@color/icon_secondary" />
    </LinearLayout>
</FrameLayout>
