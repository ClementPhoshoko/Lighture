<?xml version="1.0" encoding="utf-8"?>
<!-- Notification badge dot with a light ring so it reads on any icon. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/red_500" />
    <stroke
        android:width="1.5dp"
        android:color="@color/text_inverse" />
</shape>
