<?xml version="1.0" encoding="utf-8"?>
<!-- Soft red radial glow that sits behind the splash logo. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <gradient
        android:type="radial"
        android:gradientRadius="130dp"
        android:startColor="@color/red_glow_strong"
        android:centerColor="@color/red_glow"
        android:endColor="@android:color/transparent" />
</shape>
